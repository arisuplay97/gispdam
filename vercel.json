{
  "version": 2,
  "buildCommand": "pnpm install --no-frozen-lockfile && cd artifacts/smart-water && pnpm run build",
  "outputDirectory": "artifacts/smart-water/dist/public",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ],
  "env": {
    "NODE_ENV": "production"
  }
}
